context:
  name: mgclient
  version: "1.6.0"
  m2: ${{ "" if unix else "m2-" }}

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://github.com/memgraph/mgclient/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 64502446000d54398a4c70cf92c004ebaecb3c422139704b60d84c8d1659a171
  patches:
    - if: not unix
      then:
        - disable-static-build.patch

build:
  number: 0

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ compiler('cxx') }}
    - ${{ stdlib('c') }}
    - binutils
    - cmake >3.10
    - ninja
    - ${{ m2 }}sed
  host:
    - openssl
  run:
    - openssl
  run_exports:
    - ${{ pin_subpackage('mgclient', upper_bound='x.x') }}

tests:
  - package_contents:
      strict: true
      include:
        - mgclient.h
        - mgclient-export.h
      lib:
        - if: unix
          then: libmgclient
          else:
            - mgclient.dll
            - mgclient.lib
      files:
        - if: not unix
          then: Library/bin/mgclient.dll

about:
  homepage: https://github.com/memgraph/mgclient
  summary: 'C/C++ Memgraph Client'
  description: |
    mgclient is a C library interface for Memgraph database.
  license: Apache-2.0
  license_file: LICENSE
  repository: https://github.com/memgraph/mgclient

extra:
  recipe-maintainers:
    - MementoRC
